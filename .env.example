# API Keys Configuration Example
# Copy this file to .env and fill in your API keys
# Note: .env file is in .gitignore and will not be committed

# DashScope API Key (for Qwen and DeepSeek)
# Get from: https://dashscope.console.aliyun.com/
DASHSCOPE_API_KEY=your_dashscope_api_key_here


# ==================== 文件存储配置 ====================

# 文件存储基础目录（默认：packages/platform/backend/artifacts）
# ARTIFACTS_DIR=D:/data/artifacts

# 用户上传文件目录（默认：ARTIFACTS_DIR/user_files）
# USER_FILES_DIR=D:/data/user_files

# Agent 生成文件目录（默认：ARTIFACTS_DIR/agent_artifacts）
# AGENT_ARTIFACTS_DIR=D:/data/agent_artifacts

# 最大文件大小（字节），默认 50MB
# MAX_FILE_SIZE=52428800

# Agent 执行工作目录（默认：系统临时目录/agent_workspace）
# AGENT_WORK_DIR=D:/data/agent_workspace

# 是否保留 Agent 执行工作目录（调试用，默认：false）
# PRESERVE_AGENT_WORK_DIR=false


# ==================== 数据库配置 ====================

# 数据库连接 URL（默认：SQLite）
# DATABASE_URL=sqlite:///./lingnexus.db

# PostgreSQL（可选，推荐生产环境）
# DATABASE_URL=postgresql://user:password@localhost:5432/lingnexus

# MySQL（可选）
# DATABASE_URL=mysql+pymysql://user:password@localhost:3306/lingnexus


# ==================== API 配置 ====================

# API 基础 URL（默认：/api/v1）
# API_BASE_URL=/api/v1

# CORS 允许的源（默认：所有）
# CORS_ORIGINS=*
